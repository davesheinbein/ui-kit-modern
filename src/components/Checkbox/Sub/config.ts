// Checkbox configuration objects and constants
// (Move any config objects/constants from Checkbox.tsx here)

// Default label positions, sizes, and variants for Checkbox
export const CHECKBOX_LABEL_POSITIONS = [
	'left',
	'right',
	'none',
] as const;
export const CHECKBOX_SIZES = [
	'small',
	'medium',
	'large',
] as const;
export const CHECKBOX_VARIANTS = [
	'default',
	'success',
	'warning',
	'danger',
] as const;
